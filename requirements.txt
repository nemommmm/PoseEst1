# Core GPU pose pipeline
# Match the version used to produce the canonical local CPU baselines.
ultralytics==8.3.235
# Pin the package name required by Ultralytics as well as the version used by
# the canonical local baseline. Using the headless distribution here causes
# pip to install a second, newer opencv-python wheel through Ultralytics.
opencv-python==4.12.0.88
numpy>=1.26,<3
scipy>=1.13,<2
matplotlib>=3.9,<4
tqdm>=4.66,<5
PyYAML>=6,<7

# Alternative pose backends and deployment. RTMLib is installed with
# --no-deps by tools/setup_remote_env.sh because its broad dependencies pull
# CPU ONNX Runtime and multiple conflicting OpenCV wheels.
onnx>=1.17,<2
# 1.27 wheels require CUDA 13; the Runpod image currently provides CUDA 12.8.
onnxruntime-gpu==1.22.0

# Utilities used by evaluation/report scripts
pandas>=2.2,<3
