# 硕士论文大纲（整合简版）

**题目**：Posture and Activity Detection in Manufacturing Environment Using 3D Vision and AI

**作者**：Fanbo Meng
**机构**：Chalmers University of Technology，合作方 Viscando AB

本版以 2026 年 6 月 2 日的大纲为基础，保留原版“评估框架—实验结果”的清晰结构，并结合后续实验增加独立的 GPU 与实时实现章节。

---

## 第一章：引言

- 介绍制造环境中的人体工程学问题，以及 RULA、REBA 等人工评估方法的作用和局限。
- 说明使用相机自动分析人体姿态、关节角度和关键危险姿势的意义。
- 提出本文的实际定位：不追求医疗或实验室级精度，而是研究系统能否提供稳定、可解释并足以支持人体工程学判断的结果。
- 说明遮挡、拍摄距离、相机视角和计算速度等实际困难。

### Research Questions

- **RQ1 — Accuracy:** *How closely do stereo-based SKT and monocular FastSAM3D agree with an Xsens-derived reference in estimating ergonomically relevant human posture?*
- **RQ2 — Robustness:** *How does the performance of the two approaches vary across different distances, viewpoints, and occlusion conditions?*
- **RQ3 — Deployment feasibility:** *How feasible is the real-time deployment of the selected SKT pipeline for industrial ergonomic assessment?*

其中，RQ1 中的 Accuracy 表示与 Xsens-derived reference 的一致程度，并不表示由绝对 Ground Truth 验证的物理准确性。

- 最后说明研究范围和主要贡献；活动识别暂不作为单独的实验任务。

## 第二章：背景与相关工作

- 介绍人体工程学评估为什么依赖关节角度，并说明本文不实现完整的 RULA/REBA 自动评分。
- 说明为什么核心精度指标是关节角度而非 3D 关节位置误差（MPJPE）：工人的工效学风险由姿态决定，RULA/REBA 等方法直接按关节角度是否落入危险区间分档，角度误差与风险等级一致性有直接对应关系；而某个关节在三维空间中偏离了多少厘米，本身并不直接对应风险高低。
- 补充技术层面的原因：MPJPE 还需要先把 SKT（立体几何度量）、FastSAM3D（单目 + 人体先验尺度）、Xsens（IMU + 运动学模型）三套原理不同的系统对齐到同一坐标系和尺度，这是一个比角度计算更难、某种程度上不适定的附加问题；角度由局部三个关节点计算得到，天然不受平移、旋转和尺度差异影响，不需要先解决这一问题。
- 介绍 YOLO 在人体关键点检测中的作用，重点放在本文实际使用的 YOLOv8m 和 YOLO11l。
- 介绍双目视觉、相机标定和三角测量的基本原理，并引出 SKT。
- 介绍单目三维人体姿态估计和人体先验，并引出 FastSAM3D。
- 介绍 IMU 人体运动捕捉和运动学模型，并引出 Xsens。
- 从原理上区分三类数据来源：FastSAM3D 是“视觉观测 + 强人体先验”，SKT 是“视觉观测 + 几何测量”，Xsens 是“身体传感器 + 运动学模型”。
- 介绍 Xsens 的工作方式及局限，明确它是外部参考系统，不是绝对 Ground Truth。

## 第三章：数据与采集设计

这一章只回答：**研究获得了哪些原始数据，它们是在什么条件下采集的？** SKT 和 FastSAM3D 运行后产生的三维姿态、角度或中间文件不在本章作为“原始数据”介绍。

### 3.1 数据集与录制场景

- 介绍 2025 年数据和 2026 年 Assar 数据，包括被试、任务、动作、录制时长、拍摄距离、视角和遮挡情况。
- 说明双目相机、Webcam 和 Xsens 是否在同一 recording 中同时采集，以及各数据集在论文中的用途。
- 区分用于主要方法比较、消融实验、距离/视角/遮挡分析和部署测试的 recording，避免把不同实验条件的数据直接混在一起。

### 3.2 采集设备与原始文件

- 双目系统：左右相机的原始视频、分辨率、名义帧率、硬件 frame ID 或时间信息。
- 单目系统：输入 FastSAM3D 的原始 Webcam 视频及其分辨率、帧率和录制关系。
- Xsens：录制并导出的 MVNX 等参考数据文件、名义采样率和 calibration 记录。它是本项目接收到的原始输入文件，但并不是未经厂商算法处理的原始 IMU 信号；如何解析和使用放在第四章。
- 用一张“采集信息表”汇总设备、原始文件、分辨率、帧率、时长和同步信息，不把 SKT 或 FastSAM3D 运行后生成的 NPZ、TRC、MVNX 等文件列为原始数据。

### 3.3 相机配置与标定条件

- 说明相机位置、双目基线、内外参数、分辨率和录制设置，以及标定数据来自何时、用于哪些 recording。
- 报告必要的标定质量信息，例如重投影误差；本章只交代实验配置和标定结果，双目几何如何使用放在 4.2。

### 3.4 实验数据的用途划分

- 说明每组 recording 对应回答哪个 Research Question，以及哪些条件是受控变化、哪些因素可能相互混杂。
- 用一张简表说明每个 recording 用于角度比较、运动比较、消融实验、鲁棒性测试还是部署测试。
- 标出专门用于单自由度运动分析的受控肘关节屈伸 recording；为什么选择该动作放在 5.2 解释。

### 3.5 数据范围与限制

- 说明被试数量、动作覆盖、缺失帧、录制长度和可用 recording 等限制。
- 说明距离、视角、动作和数据年份之间可能存在的混杂关系，因此哪些结果属于受控比较，哪些只能作为观察性证据。
- 说明实验没有独立的光学 Ground Truth；Xsens 是同步采集的外部参考，而不是绝对真值。

## 第四章：方法与统一处理流程

这一章回答：**三类原始输入如何经过各自系统，最终变成可以在同一框架下比较的角度与运动序列？**

### 4.1 整体流程与三种系统的角色

- 用一张总流程图表示：原始输入 → 方法特有的姿态/角度输出 → 共同关节定义与时间对齐 → 可追溯的共同角度序列 → 绝对角度与运动两个评估分支。
- 明确三种系统的性质：SKT 是“视觉观测 + 几何测量”，FastSAM3D 是“视觉观测 + 人体先验”，Xsens 是“身体传感器 + 运动学模型”。
- 强调 SKT 和 FastSAM3D 是被比较的视觉方法；Xsens 是外部参考系统，不作为第三个候选视觉方法参与排名。

### 4.2 SKT：视觉观测与几何测量

- 说明原始双目视频如何经过图像校正、YOLO 关键点检测、左右对应、三角测量和后处理，得到三维关节点及角度。
- 说明使用的模型和配置，以及 SKT V1/V2 的主要处理差异；具体提升结果留在第六章消融实验。
- 说明置信度、极线误差和重投影误差等几何质量信息如何产生，以及小幅 2D 误差如何在三角测量和角度计算中被放大。

### 4.3 FastSAM3D：视觉观测与人体先验

- 说明原始单目视频如何经过 FastSAM3D 产生三维人体姿态，以及人体形状和运动先验在该方法中的作用。
- 说明本文实际获得并使用的 FastSAM3D/EasyErgo 输出（保留路线以最终 MVNX 为主）、帧率、关节定义、尺度和坐标约定，以及已知的上游处理。
- 说明其输出与 SKT 不完全等价，以及无法方便提取所需中间结果用于双目三角融合的限制。

### 4.4 Xsens：参考系统及其数据处理

- 说明 Xsens 如何结合 IMU 观测、人体模型和系统 calibration 产生姿态与关节角度，以及系统内部处理可能带来的初始偏差和平滑效应。
- 说明 MVNX 数据如何被解析，并区分 Xsens native angle 与从 Xsens 三维点重新计算的 Xsens-derived geometric angle。
- 解释为何主要比较采用 Xsens-derived reference，以及为什么它仍不能被称为绝对 Ground Truth。

### 4.5 共同关节与角度定义

本节把"角度"这件事一次性定死，后续各章不再重新解释或限定。

- 说明三种系统之间的关节映射、坐标和单位约定，以及哪些关节能够可靠对应。
- **统一的角度公式**：每个关节角定义为三点链中间关节处的夹角（例如肘部用 shoulder–elbow–wrist），由三维坐标计算。SKT、FastSAM3D、Xsens 三方使用**完全相同的函数**，因此报告出来的差异反映的是底层三维位置的差异，而不是角度定义口径不同。
- **四个命名量**，从第五章起全文统一使用：
  - $\theta_{\text{SKT}}$、$\theta_{\text{FS}}$ —— 两个被评估的视觉方法
  - $\theta_{\text{XS-geo}}$ —— 由 Xsens 三维节段位置用同一三点公式重算得到的几何角，**本文所有精度陈述的参考基准**
  - $\theta_{\text{XS-nat}}$ —— 厂商自带的 native 关节角，**仅用于界定参考系统自身的分辨率下限**（见 5.3），不作为精度基准
- 说明为什么用 $\theta_{\text{XS-geo}}$ 而不是 $\theta_{\text{XS-nat}}$ 作基准：只有几何重算版本与视觉方法共享同一角度定义，不会把厂商特有的关节约定混进比较里。
- 记录参考口径一致性审计（2026-09-11）：2026 的评估运行没有配置 fair-angle 资产，代码静默回退到了 $\theta_{\text{XS-nat}}$。本文所有 2026 结果均按 2025 的定义重新计算 $\theta_{\text{XS-geo}}$，视觉方法输出和时间 offset 保持不变，修正后的数值见 6.3。
- 说明缺失关键点、异常值、共同有效帧和短缺口插值规则。

### 4.6 时间同步与采样

- 先根据硬件 frame ID 建立 stereo camera timeline，再把 FastSAM3D 和 Xsens 映射到共同时间轴。
- FastSAM3D 根据帧索引或时间信息映射；Xsens 的 60 Hz 信号通过插值采样到相机时间点，而不是简单地每隔若干帧丢弃数据。
- 说明每个 recording 的时间 offset 如何确定，并通过角度曲线、录制前后片段和持续时间检查残余错位或时钟漂移。
- 记录每个 recording 实际使用的系统输出来源和必要预处理。本文比较的是各系统在当前数据条件下产生的最终角度结果，不把不同 recording 中可能存在的上游处理差异解释为独立的滤波效果。
- 说明本项目唯一额外施加的滤波步骤，以及为什么它是"补偿"而不是"增强"：FastSAM3D 的输出由上游厂商流程交付时已经过内部滤波，本研究无法获得其未滤波版本；因此对两种视觉方法统一施加一个轻量、相同的移动平均（约 240 ms，12.5 FPS 下为 3 帧），以部分抵消这一无法控制的上游不对称。Xsens 除插值到相机时间轴外不叠加任何项目级处理。该操作是均值保持的，系统间的恒定角度偏移不会被它改变。
- 各系统输出经过关节映射、时间对齐、共同有效帧、明确的缺失值处理和上述统一滤波，形成每个 recording 的共同角度序列；**绝对角度和运动结果都从这同一条序列产生，两者的唯一区别是要不要做差分**。

## 第五章：评估框架

这一章只回答一个问题：**如何公平、完整地评价 SKT 和 FastSAM3D？**

正文承接前文已定义的方法与缩写，不重新介绍三种系统或预告后续章节。开头放评估流程图：处理后的角度序列 → 绝对角度 / Delta 两条分支 → 各自共同有效样本上的指标，并结合各方法自身 coverage 解读。不要在计算 Delta 前删除缺失行。

**三种比较模式，在此一次性定义清楚。** 后续章节用到三类性质不同的比较，混用会导致错误的精度声明：

- **模式 A —— 方法 vs 参考**（$\theta_{\text{SKT}}$ 或 $\theta_{\text{FS}}$ 对 $\theta_{\text{XS-geo}}$）。**只有这一模式支持精度陈述**，用于回答 RQ1，出现在 6.1–6.3。
- **模式 B —— 方法 vs 方法**（$\theta_{\text{SKT}}$ 对 $\theta_{\text{FS}}$）。不是精度指标，因为两边都不是参考。它只在"比较对象在所有被比较项之间保持不变"时可用——此时**排序有意义、绝对水平无意义**：这正是 6.4 消融的情形（四种 pipeline/检测器配置对同一条不变的 FastSAM3D 轨迹排序）。绝不可把模式 B 的数值当作相对真值的误差引用。
- **模式 C —— 参考系统与自身比较**（$\theta_{\text{XS-nat}}$ 对 $\theta_{\text{XS-geo}}$）。给出参考系统究竟能分辨多小差异的可测下限：当某个视觉方法的模式 A 差异接近这一下限时，用这个参考已无法再证明进一步的改善。

### 5.1 角度维度

- 比较逐帧绝对关节角度，直接回答视觉方法给出的姿势与 Xsens-derived reference 有多接近。
- 以 Median absolute error 和 MAE 表示典型与整体差异，以 p95 和箱线图展示误差尾部与异常值；箱线图是展示方式，不是独立 metric。
- 使用 Bias 检查持续高估或低估，并在角度随时间曲线中保留不同系统之间的角度 offset，而不为了提高一致性而先将曲线归零。
- 使用人体工程学角度区间一致性作为应用层指标，但不称为完整 RULA/REBA 自动评分。
- 结合 Xsens 的 calibration 偏差解释结果，不把观察到的角度差异直接称为绝对物理误差。

### 5.2 运动维度

- 解释为什么 MAE 不能单独回答运动问题：绝对角度会受到 Xsens 初始偏差影响，因此还需要比较姿态随时间的变化；这不是放弃 MAE，而是增加一个互补视角。
- 以肘关节屈伸这一简化的单自由度动作为重点，使用 K-frame Delta Angle 表示一段明确时间间隔内的角度变化，并把 K 换算成实际时间。
- 说明选择单自由度肘屈伸的原因：角度定义直观、三种系统都能使用 shoulder–elbow–wrist 链计算，并且受控动作有助于先隔离时间对齐和抖动问题；同时说明结论不能直接推广到复杂多自由度动作。
- 以 Delta MAE 表示运动变化量相差多少，以 Pearson correlation 表示变化方向和趋势是否一致；二者必须结合解释，因为高相关性并不等于变化幅度准确。

### 5.3 公平比较与可靠性说明

- 明确当前不存在完美的比较标准：绝对角度容易暴露 Xsens 的 calibration offset，而 Delta Angle 又会放大视觉系统的逐帧 jitter；因此两类结果必须并列报告，不能用其中一种掩盖另一种的弱点。
- 对承担主要结论的 recording，尽量同时展示绝对角度与 motion 结果：前者说明姿势数值和基准偏差，后者说明变化趋势和动作幅度；不要求每个诊断、消融或鲁棒性 recording 都重复完整的两套分析。
- 所有方法使用统一时间范围、共同有效帧和统一时间对齐；同时分别报告各方法自身的有效帧比例，避免只在最容易的帧上比较精度。
- 报告缺失关键点、异常跳变和几何质量信息，用于解释结果是否稳定以及失败发生在哪里。
- 每项结果注明数据、关节、参考方式和配置，避免把不同 recording、不同 filter 或不同模型设置下的数字直接混合排名。
- 骨长、人体比例、度量尺度和坐标差异仅作为输出解释与部署讨论，不再构成独立的位置精度评价维度；MPJPE 等 3D 关节位置误差指标不作为本文的正式精度指标，理由见第二章。
- 报告模式 C 的分辨率下限作为一条具体边界（native 与几何角的分歧在肩部最大、膝部最小），所有模式 A 的结果都要对照这条线来解读。
- 报告一次滤波敏感性检查，避免第四章那个统一滤波被误读成"为了让结果好看"：若完全去掉该滤波，SKT 的 Delta MAE 上升约五分之一，而 FastSAM3D 只上升几个百分点。**这个不对称本身就是证据**——两种方法在逐帧抖动上的差距，有一部分来自本项目上游的厂商处理，而非方法本身。

**第六章写作备注（不属于第五章正文）：** 运动结果可使用三面板散点图比较 **Delta Xsens–Delta SKT**、**Delta Xsens–Delta FastSAM3D** 和 **Delta SKT–Delta FastSAM3D**。三幅图使用统一坐标范围，并显示理想一致线 `y = x`、线性拟合线和 Pearson correlation。前两组评价视觉方法与参考系统的运动一致性；第三组只说明两种视觉方法是否产生相似变化，不作为准确性排名依据。

## 第六章：实验结果

这一章只保留一条容易理解的主线：先在同一段 2025 长序列上回答绝对角度和运动一致性，再**在同一段录制内部**考察两种方法对距离的依赖、并用两段 2026 右肘视频交叉验证，最后用消融实验说明为什么选择当前的 pipeline 和检测器。Xsens 仍然是外部参考系统，因此本章报告的是一致性，而不是已知的物理真值误差。除非小节另行说明，所有精度陈述均为模式 A（对 $\theta_{\text{XS-geo}}$）；全章使用的配置依据见 6.4。

| 小节 | 主要数据 | 在论文中的作用 |
|---|---|---|
| 6.1 | 2025 Aitor 长序列，8 个关节 | 回答绝对角度与 Xsens reference 的一致程度 |
| 6.2 | 与 6.1 相同的 2025 数据，左右肘 | 回答两种方法能否跟随动作变化 |
| 6.3 | 2025 录制内深度扫描（主证据）+ Fanbo7/Fanbo4 右肘（交叉验证） | 回答 RQ2 的距离依赖，并给出两种方法差异的机制解释 |
| 6.4 | 2026-07-07 的 2×2×5 消融实验 | 区分 pipeline 与检测器选择的影响 |
| 6.5 | 6.1--6.4 的结果 | 综合回答 RQ1 和 RQ2，不增加新的实验 |

### 6.1 绝对角度一致性：2025 主实验

- 只使用冻结的 **V2 + YOLOv8m** 输出，在 2025 Aitor 长序列上比较 SKT、FastSAM3D 与 Xsens reference。
- 两种视觉方法**各自**与 $\theta_{\text{XS-geo}}$ 比较（模式 A）。并列表格限制在两种方法都有效的共同时间点上，避免任一方通过丢弃困难帧获得较低误差，并在同一张表里报告各自的 coverage。这里**不是**把 SKT 与 FastSAM3D 直接相减。
- 正文用一张按关节 MAE 表保留具体数值，配一组“误差分布 + coverage”图说明典型差异、尾部与可用性；不为每个指标分别制图。
- 人体工程学角度区间一致率作为补充结果用一句话报告，不扩展为完整 RULA/REBA 评分。
- 这一节只回答“角度水平与参考系统有多一致”。不能把与 Xsens 的差值写成已知的物理误差。

### 6.2 运动一致性：与 6.1 成对解读

- 使用与 6.1 相同的 2025 Aitor 数据，只评价左右肘，并固定使用第五章定义的 **K=6**；不再同时展示多个 K。
- 使用一张三面板相关性图：**Delta Xsens vs Delta SKT**、**Delta Xsens vs Delta FastSAM3D**、**Delta SKT vs Delta FastSAM3D**。各面板显示 `y = x`、线性拟合线和 Pearson correlation，并使用相同坐标范围。
- 用一张简短表格报告 SKT 和 FastSAM3D 相对于 Xsens-derived reference 的 Delta MAE 与 Pearson correlation。Delta MAE 回答变化量相差多少，Pearson correlation 回答变化趋势是否一致；第三组视觉方法间相关性只用于解释两种方法是否以相似方式响应运动。
- 必须将真实角度曲线和 Delta 曲线成对展示。保留 0511 报告曾查看的 22.5–43.8 s 右肘窗口，但用当前输出重绘；同时展示全序列背景，窗口仅用于解释，不据此选取定量结果。
- 本节结论只回答：哪种方法的变化幅度更接近参考、哪种方法更能跟随变化趋势。相关性不单独解释为准确性，也不替代 6.1 的绝对角度结果。

### 6.3 距离依赖与录制条件

本节回答 RQ2。主证据是**录制内**的距离扫描，因为这是本研究中唯一一处"被试、session、相机、标定全部固定、只有距离在变"的条件。

- **主证据 —— 2025 录制自身的深度扫描。**被试躯干深度在**6.1/6.2 已经在用的同一段录制内**从约 2.8 m 变化到 6.4 m（p10–p90），因此不引入任何新数据。把两种方法的逐帧模式 A 差异按估计躯干深度分箱，报告每个箱的八关节平均值、帧数和 coverage。
- 结果按测量原理把两种方法分开了：**SKT 的差异随距离显著增大**（3–4 m 段约 16°，7 m 以外约 28°），而 **FastSAM3D 在同一区间基本持平**（两端都约 13°）。两者的差距因此从近距约 5° 扩大到远端约 14°。
- 把这解释为两种测量原理的必然结果，而不是笼统的优劣排名：立体深度不确定度随距离平方增长，且必须先进入三维位置估计才能传到角度；而带人体先验、由外部给定身体尺度的模型拟合，其结果基本与距离无关。**这就是 6.5 中总体排序背后的机制。**
- 如实说明横轴的局限：深度由被评估的 SKT 重建自身估计得到，因此不是独立测量。但这是唯一可得的口径——FastSAM3D 的尺度来自人工输入的身高，Xsens 没有相对相机的位置。使用躯干质心而非单个关节以提高稳定性，并说明第四章的物理有效性边界已剔除会干扰分箱的远端异常深度。
- **跨录制交叉验证 —— Fanbo7（约 1.8 m）与 Fanbo4（约 4.1 m）。**两段都含右肘屈伸，都用 V2 + YOLOv8m 并在共同有效帧上比较。用一组 0629 原始场景图、一组右肘时间曲线和一张紧凑表交代条件、偏差形态、数值与各方法自身 coverage；不增加其他 recording。
- 按第四章定义的参考口径审计修正后的数值：Fanbo7 为 SKT 6.68° / FastSAM3D 5.01°，Fanbo4 为 11.49° / 7.36°。来源和计算保存在 MasterThesis/figure/chapter6/evidence_manifest.json，原实验文件不覆盖。
- 两种方法在 Fanbo4 的差值都更大，方向与录制内扫描一致；但两段之间动作幅度、时长、视角和图像尺度也不同，因此这一对数据**只作为方向上的佐证**，不作为距离效应的独立测量。
- Viewpoint 和 occlusion 没有满足共同 reference 与受控变量要求的定量结果，在第八章作为限制说明。

### 6.4 Pipeline 与检测器消融实验

- 开头用一句话说明：五个场景中有两段录制（Fanbo3 走路、Fanbo9 双相机 session）只在本节出现，并各用一句交代它们提供了什么，避免读者在消融实验里第一次见到这两个数据集时没有背景。
- 使用 2026-07-07 的 **V1/V2 pipeline × YOLOv8m/YOLO11l × 5 个场景** 矩阵，正文用两面板柱状图分别展示“固定 YOLOv8m 更换 pipeline”和“固定 V2 更换模型”。完整矩阵留在可追溯数据中，避免表图重复。
- 本节是全章**唯一使用模式 B**（SKT vs FastSAM3D）的地方。之所以成立，是因为四种配置面对的是同一条不变的比较轨迹，因此**排序**可解释，即使**绝对水平**不是精度数字。这些数值不得带入 6.1--6.3。
- 主动堵住循环论证的质疑：用 FastSAM3D 当标尺选出 SKT 的配置、再拿 SKT 与 FastSAM3D 比较，只有在「排序依赖于标尺选择」时才构成循环。实际并不依赖——在同时具备参考系比较的 Fanbo9 双相机 session 上，**八个配置 cell 在两种比较标尺下给出的检测器排序完全一致**。这一句要主动写出来，不要留给答辩老师来问。
- 只保留两个稳健结论：在相同 YOLOv8m 下，V2 在五个场景中都降低或基本保持该差值；在 V2 下，YOLOv8m 在 3/5 场景更好，YOLO11l 在另外两项只领先 0.05° 和 0.78°。
- 因此当前选择 **V2 + YOLOv8m** 是跨场景的工程默认配置，而不是宣称 YOLOv8m 在所有条件下都更准确。
- 17 组 filter ablation、位置层各阶段调整和失败的融合路线不放入第六章主结果；如需要，可移至附录或第八章作为开发过程和限制。

### 6.5 结果总结与证据边界

- 不增加新表格或新数据，只用短段落把 6.1--6.4 对应回 RQ1 和 RQ2。
- **RQ1**：在 2025 主实验中，FastSAM3D 整体上与 Xsens reference 更接近、运动相关性更高且 coverage 更完整。**在同一段 2025 录制上** SKT 在左右肘的绝对 MAE 略低；而在两段 2026 右肘录制上 FastSAM3D 在肘部同样更低。**每一句这类结论都必须绑定到具体录制**，不能把某一段数据上的肘部结果写成一般性结论。
- 用测量原理解释这个排序，而不是停留在「谁的数字更小」，两条机制在 6.3 都有直接证据：
  - **强人体先验 + 外部给定的身体尺度**会把解约束住，与被试在画面中占多少像素基本无关——这就是 FastSAM3D 的差异在 2.8–6.4 m 深度扫描上近乎持平的原因。
  - **SKT 做的是几何测量，必须先估计三维位置才能导出角度**，因此随距离平方增长的立体深度不确定度会先进入位置、再传播到角度——这就是 SKT 的差异在同一扫描上显著增大、两者差距随距离拉开的原因。
- 简短补一条解读边界，不要过度发挥：FastSAM3D 与 Xsens 都属于「拟合人体模型」一类，而 SKT 是三者中唯一的直接几何测量，因此「与这一特定参考系统的一致程度」本身并不是一个完全中立的评判标准。把它写成对「这个比较能证明什么」的限定，而不是用来取代上面两条机制解释。
- **RQ2**：6.3 的录制内深度扫描**确实**在被试、session、相机、标定全部固定的条件下分离出了距离效应，两段 2026 录制在方向上佐证了这一结果。Viewpoint 与 occlusion 仍未解决，移交第八章。
- 消融实验说明最终 pipeline 的选择来自跨场景稳定性，而不是单一视频上的最低数字。
- 在角度结论之外保留 SKT 的方法级优势：具有真实度量尺度的位置输出、可以逐帧报告自身何时不可靠的几何质量信号，以及不依赖外部服务的本地部署能力。本章以角度为中心的实验并未直接验证位置精度，部署维度由第七章回答。
